version: "3"
services:
  do:
    build:
      context: ./
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      - TZ=Asia/Tokyo
    volumes:
      - ./vuln_info_bot:/app/vuln_info_bot
